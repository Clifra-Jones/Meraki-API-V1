# Public enums  and variables

enum CategoryListSize {
    topSites;
    fullList
}

enum productTypes{
    wireless;
    appliance;
    switches;
    systemManager;
    camera;
    cellularGateway
}
